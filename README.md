# This is a practice for ORM(hibernate) and JDBC to connect to the data base


*DriverManager* : Creates a connection to the database using a URL, username, and password.

*Connection* : Represents the session with the database; used to create statements.
